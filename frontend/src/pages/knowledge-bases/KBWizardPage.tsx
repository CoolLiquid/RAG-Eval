import { PagePlaceholder } from '@/components/PagePlaceholder';

export function KBWizardPage() {
  return (
    <PagePlaceholder
      title="挂载 MCP 知识库"
      description="4 步向导：连接 → 选择 Tool → 试检索 → 保存"
      priority="P0"
    />
  );
}
